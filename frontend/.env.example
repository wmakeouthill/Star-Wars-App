VITE_API_BASE_URL="http://localhost:8000"
VITE_GOOGLE_CLIENT_ID=""

# Usado pela Vercel Function `frontend/api/[...path].js` (proxy autenticado do Cloud Run).
# Configure isso no painel do Vercel (Environment Variables).
CLOUD_RUN_URL="https://star-wars-backend-506724076071.southamerica-east1.run.app"

# JSON inteiro da Service Account com role `roles/run.invoker`.
# Configure no Vercel como string (uma linha).
GOOGLE_SERVICE_ACCOUNT_KEY=""

# (compat) se você já usa essa env var, o proxy também aceita.
BACKEND_BASE_URL="http://localhost:8000"

